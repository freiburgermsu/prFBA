# Large regenerable data artifacts — exceed GitHub's 100 MB file limit.
# Rebuild with:  python embed_16s.py   (then  python analyze_space.py)
embeddings.f16.npy
*.parquet
sort_order.npy
umap_coords.npy
umap_row_ids.npy

# Runtime state / logs / sentinels
progress.json
run.log
synth_build_run.log
DONE
FAILED

# Python caches
__pycache__/
*.pyc
.ipynb_checkpoints/

# macOS junk
.DS_Store
._*

# Region-matched (V4-V5) store build — regenerable via insilico_pcr.py + embed_16s.py
v4v5_refs.json
v4v5_embed.log

# NCBI taxdump (downloaded by taxopy for concordance.py; regenerable)
*.dmp
taxdump*.tar.gz

# old-ASV top-100 full hits (100MB; regenerable via hit_amplicons.py --topk 100)
old_asv_comparison/asv_top100_hits.json

# BV-BRC download cache: per-genome KBase JSONs + PGFam gene-family sets
# (regenerable via prefetch_gene_families.py + build_synthetic_genomes_parallel.py)
bvbrc_cache/
genome_objects/
ASVset_taxonomies/
