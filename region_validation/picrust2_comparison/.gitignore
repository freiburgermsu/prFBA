# PICRUSt2 comparison bulk artifacts (regenerable via scripts/, see README.md).
# Tracked: scripts/, README.md, and the distilled summaries whitelisted below.
data/*
!data/.gitkeep
!data/*summary*.json
!data/*summary*.csv
!data/annotation_ceiling.json
!data/domain_routing.csv
!data/prfba_selection_meta.json
# figures are small and tracked, as in region_validation/figures/

# depth probe: keep the distilled result + sample manifest, not the 89 MB re-alignment
!data/depth_probe/
data/depth_probe/*
!data/depth_probe/depth_probe_results.json
!data/depth_probe/wiped_sample.keys.json
